<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/background">
        <shape>

            <corners android:radius="5dip" />

            <gradient
                android:angle="270"
                android:centerColor="@color/crs_primary_color"
                android:centerY="0.50"
                android:endColor="@color/crs_primary_color"
                android:startColor="@color/primary_material_dark" />
        </shape>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="90"
                    android:endColor="@color/primary_material_light"
                    android:startColor="@color/crs_primary_color" />
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="90"
                    android:endColor="@color/primary_material_light"
                    android:startColor="@color/crs_primary_color" />
            </shape>
        </clip>
    </item>

</layer-list>